<!DOCTYPE html>
<html>
    <head>
        <title>{% block title '' %}Monitor treningu sportowego</title>

        <link href="{{ asset('css/main.css') }}" rel="stylesheet" type="text/css" />

        <script type="text/javascript">
            var _gaq = _gaq || [];
            _gaq.push(['_setAccount', 'UA-XXXXXXXX-X']);
            _gaq.push(['_trackPageview']);

            (function() {
                var ga = document.createElement('script'); ga.type = 'text/javascript'; ga.async = true;
                ga.src = ('https:' == document.location.protocol ? 'https://ssl' : 'http://www') + '.google-analytics.com/ga.js';
                var s = document.getElementsByTagName('script')[0]; s.parentNode.insertBefore(ga, s);
            })();
        </script>
    </head>
    <body>
        <nav class="navbar navbar-default">

            <ul>
                <li><a href="{{ url('index' )}}">Home</a></li>
                {#bind#}

                <li><a href="{{ url('auth_login') }}">Login</a></li>

                <li><a href="{{ url('auth_register') }}">Register</a></li>

            </ul>


                {% if is_granted("ROLE_USER") %}

                    <ul>
                        <li><a href="{{ url('auth_logout') }}">Logout</a></li>
                        <li><a href="{{ url('index') }}">Change password</a></li>
                        <li><a href="{{ url('calendar') }}">Calendar</a></li>

                    <li class="dropdown">
                        <a href="javascript:void(0)" class="dropbtn">Training</a>
                        <div class="dropdown-content">
                            <a href="{{ url('show_week_training') }}">Show last 5 trainings</a>
                            <a href="{{ url('show_all_training') }}">Show all</a>
                            <a href="{{ url('add_training') }}">Add training</a>
                            <a href="#">Link 3</a>
                        </div>
                    </li>


                    <li class="dropdown">
                        <a href="javascript:void(0)" class="dropbtn">Training Day</a>
                        <div class="dropdown-content">
                            <a href="{{ url('add_training_day') }}">Add training day</a>
                            <a href="{{ url('show_all_training_day') }}">Show all</a>
                        </div>
                    </li>
                    </ul>

                {#{% elseif  is_granted("ROLE_ADMIN") %}#}
                <ul>

                <li class="dropdown">
                    <a href="javascript:void(0)" class="dropbtn">Admin</a>
                    <div class="dropdown-content">
                        <a href="{{ url('add_sport') }}">Add sport name</a>
                        <a href="{{ url('show_all_users') }}">Show all users</a>
                        <a href="{{ url('show_all_trainings') }}">Show all trainings</a>
                        <a href="{{ url('show_all_training_days') }}">Show all training days</a>
                        <a href="{{ url('show_all_sport_names') }}">Show all sport names</a>

                    </div>
                </li>

                </ul>

                {% endif %}


            <h1>Monitor treningu sportowego</h1>

            <h3>„W konfrontacji strumienia ze skałą,<br>
                strumień zawsze wygrywa -<br>
                nie przez swoją siłę, <br>
                ale przez wytrwałość." <br>
                ~Buddha</h3>

            <div class="parallax"></div>

            <div style="height:50px;">

            </div>

            <div class="container-fluid">
                <div class="btn-group pull-right" role="group" aria-label="{{ 'label.actions'|trans }}">
                    {% if is_granted('IS_AUTHENTICATED_FULLY') %}
                        <a class="btn btn-default" href="{{ url('auth_logout') }}" title="{{ 'auth.logout.label'|trans }}">
                            {{ 'auth.logout.label'|trans }}
                        </a>
                    {% else %}
                        <a class="btn btn-default" href="{{ url('auth_login') }}" title="{{ 'auth.login.label'|trans }}">
                            {{ 'auth.login.label'|trans }}
                        </a>
                    {% endif %}
                </div>
            </div>
        </nav>

        {#{% include 'default/_flash_messages.html.twig' %}#}

        {% block content %}{% endblock %}

    <br>
    <br>
    <br>
    <br>

    <footer> Autor: Katarzyna Okońska 2017</footer>
    </body>
</html>
